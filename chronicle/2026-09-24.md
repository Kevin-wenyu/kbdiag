# 2026-09-24

## 1.3 切换（用户确认"开始 1.3"）

- 归档提交 `6015c47`：把工作区全部提交，包括 Go 代码、活文档、chronicle，以及当时还没跟踪的旧文档、shell 时代的计划和 agents。提交前扫过一遍密钥（没有发现；`kbdiag_ro` 的测试密码本来就公开写在 CLAUDE.md），也没有超过 1MB 的文件
- 打了附注 tag `shell-final`，指向 `6015c47`
- 从 main 删除：`lib/`、`dist/`、`test/`、`build.sh`、`kbdiag.sh`、`scripts/{deploy.sh,setup-dev.sh,hosts.example,init-docs.py}`、`.claude/agents/`（两个 shell 版用的）、`docs/rewrite/`、`docs/superpowers/`、`docs/test-report-2026-07-09.md`、`docs/dba-scenario-acceptance.md`、`docs/problem-signal-extraction-2026-09-08.md`、`docs/docs-site-setup.md`、shell 时代的 superseded 计划；空目录 `docs/adr/` 也删了
- 计划清单之外多删了一个根目录的 `CONTEXT.md`：它是 shell 版的术语表，而 CLAUDE.md 规定不建 CONTEXT.md。内容保存在 tag 里
- 删除前 grep 过：活文档和代码里对这些路径的引用，只剩旧 CI 和 README
- CI（`.github/workflows/ci.yml`）改成一个 job：setup-go（版本取自 go.mod），然后跑 `go vet`、`go test ./...` 和文档数检查。`.git/hooks/pre-commit` 改成本机跑同样三项；它不在版本库里，Codex 那边要用得自己装。反向验证过：多放一个 `docs/zombie.md`，hook 以 exit 1 拦下
- README 顶部加了中英过渡说明：v2.0.0-alpha.1 之前 main 上只有 `sessions`，下文仍是 shell 版说明，要完整功能请用 `v1.0.0`。README 按计划到阶段 3 才重写
- **N5：从 1.3 到阶段 3 之间，main 上只有 `sessions` 可用，其余命令陆续加入；需要完整旧功能请用 `v1.0.0`**
- 通过标准核对：`git ls-files lib dist test build.sh kbdiag.sh` 无输出；文档数检查通过；pre-commit（vet+test+文档数）通过。1.2 的 VM e2e 结果见 2026-09-23 的记录（node1、node2 全绿）；这次切换只删了 shell 文件，没有改 Go 代码

## sessions 文档页（kbdiag-docs `v2`）

- 按 84c883e 交叉编译（linux/amd64，CGO_ENABLED=0），部署到 node1、node2 的 `~kingbase/kbdiag`
- 在 node1 上用 `idle_txn.sh`、`long_query.sh` 注入后采样，采样时间 2026-09-24 00:09 +08:
  - 默认输出（`--limit 6`）：OK，exit 0
  - `--active --idle-in-txn-warn 5`：WARN，exit 1。被 `--active` 隐藏的 320047 照样报 WARN
  - `kbdiag_ro` 远程连接：UNKNOWN，exit 3，12 行的 9 列被遮
  - 采样后已执行两个注入脚本的 `down`
- kbdiag-docs 新增 `content/docs/reference/sessions{,.zh}.md`；`scripts/check-site.py` 的 CORE 加了 `docs/reference/sessions/`，证据标记为 `84c883e` 和 `320047`
- 本机按 CI 同样参数跑了 `hugo`（0.165.0 extended），以及 `unittest`（7 个）和 `check-site.py`（45 页，0 失败）
- 已在 `v2` 提交 `8d59224`。用户确认后推送，远程新建了 `v2` 分支。推之前核对过：kbdiag-docs 的两个部署流程都只在推 main 时触发，线上站点仍是 v1
- 已知缺口：
  - reference 的 `_index` 仍是 shell 版 README 的快照，没有链到 sessions 页，按计划到发布时再处理
  - finding 的 symptom 文字目前是中文，英文页已注明

- 用户定下规则：文档和代码保持同步。代码推到 main 后，对应的文档页也推到 kbdiag-docs `v2`，已写进 CLAUDE.md

## 阶段 2：其余 6 条命令（session、locks、txn、waits、status、slots）

用户睡前授权：自主推进到 v0.1 基线，需要时直接让 Codex 审；不越过 tag、docs 合 main、L6 验收三道门。

- 代码（未提交时的检查点）：`internal/probe/{probe,lock,txn,wait,inst,slot}.go`，`internal/facts/{lock,txn,wait,inst,slot}.go`，`internal/rule/{lock,txn,inst}.go` 及单测，`internal/scenario/{lock,txn,inst}.go` 及 PRD 金样测试，`cmd/kbdiag/main.go` 注册 7 条命令
- 注入：`lock.sh` 加备库分支（advisory lock 424242）；新增 `prepared_waiter.sh`（2PC 挡路，blocker pid 0，仅主库）、`standby_slot.sh`（备库上的 inactive 槽，验证按回放 LSN 算保留 WAL）。三个脚本都在 VM 上 up/down 过
- e2e：新增 `e2e/commands_test.go`。node1 第一次跑 24 过 2 挂，都是测试自己的错：ksql 的 `::bigint` 会把启动时间四舍五入（改成 `floor`）；布尔值拼成文本是 `true` 不是 `t`。改完后这两个测试在 node1 重跑通过
- node2 第一次跑 `TestSessionsRedactedForNonMonitorUser` 挂在 `idle_txn up` 的 `wait_for` 超时上，但从测试开始到失败只有约 4 秒，而超时设的是 15 秒。单独跑能过，整套重跑 25 个全过。怀疑 VM 时钟跳变导致 `SECONDS` 跳（重跑后 chronyc 显示时钟正常），没有证实，记为间歇性问题
- 手工核对：`kbdiag_ro` 在 node1 上跑 `status`，5 个库都拿得到大小，OK，exit 0。所以"没有 CONNECT 权限时大小为 NULL"只有 L2 覆盖
- 文档：`queries.md` 的 6 行追溯表改成已验证，并加注记（wait_s 近似、2PC 挡路、备库分支、各项缺口），阈值表加 txn/lock 三行；`engineering.md` §7 的注入表加了 3 个脚本
- 新增 L5 第 5 格：`TestNonMonitorUser/with_sys_monitor_nothing_is_redacted`，在主库上 `grant sys_monitor to kbdiag_ro`，结束时 revoke；断言 sessions/session/locks/txn/waits 的 `redacted[]` 都为空，locks 里 waiter 的 wait_s 拿得到。备库跳过：备库上不能 GRANT，主库上的授权会复制过去
- **间歇性失败的根因找到了**：用户睡觉后 Mac 空闲休眠，Lima VM 跟着挂起，醒来时 chronyd 记"Forward time jump detected"，bash 的 `SECONDS` 和 KES 的 `now()` 一起跳。host 连续两次 5 秒采样之间 host 自己的时钟跳了 600 秒，这是直接证据。解释了：`wait_for` 在远小于超时的时间内就报超时；session 测试里 `now()-state_change` 算出来不足 1 秒所以没 finding；slot 注入要等约 30 秒，最容易撞上。之后的 VM 测试一律用 `caffeinate -i` 包起来跑
- Codex 审查（`codex exec -s read-only`）：只有 1 条 P2——`TestNonMonitorUser` 里 status/slots 那一格只排除了 exit 3，WARN/FAIL/64 都能通过。Jev 分级：invalid 0.68、valid 0.31，严重度 2.1。人工核对：**意见成立**（Jev 判错），已改为断言 OK/0。Codex 总结论：没发现其他有充分证据的契约、SQL、边界或文档问题
- `caffeinate -i` 挡不住：pmset 日志里 03:30 仍然 "Entering Sleep state due to 'Maintenance Sleep'"。改用 `caffeinate -dimsu` 后，最终代码上两节点全绿：node1 `ok 125.1s`（`-i` 那一轮恰好没休眠），node2 `ok 78.9s`。node2 在 `-i` 那一轮挂过一次 `TestSessionsIdleInTxn/below_the_default_threshold`（刚 idle 几秒的会话在 300 秒默认阈值下被报 WARN，就是时钟前跳了 300 秒以上），`-dimsu` 重跑通过。engineering.md §8 加了这条注意事项
- L1/L2：`go vet ./...`、`go test ./...` 通过；文档数检查通过
- 阶段 2 已提交推送：`0a4d61e`

## 阶段 3：文档、README、构建、L6 步骤（停在审批门前）

- 在 node1/node2 上用 `0a4d61e` 的 amd64 二进制重采了 6 条命令的样例（04:38–04:41 +08），注入脚本采样后都已 `down`
- kbdiag-docs `v2`：新增 `content/docs/reference/{session,locks,txn,waits,status,slots}{,.zh}.md`，sessions 页去掉"目前唯一命令"那句并链到 session；`check-site.py` 的 CORE 加 6 条（证据标记 `0a4d61e` 加各页的 pid/值）。提交 `881bde3` 并推送
- Codex 审 881bde3（只读）：3 条 P2。Jev 分级与人工结论：
  - waits 页说"采到就是 OK"，但 `rule/inst.go` 有遮蔽时给 UNKNOWN。Jev valid 0.94；人工：成立
  - txn 页说"只列在事务里的会话"，实际还列看不到状态的会话。Jev invalid 0.55；人工：下一句已补充，但首句确实有歧义，照改
  - slots 页说 xmin 来自 hot_standby_feedback，probe 是通用读取 `sys_replication_slots.xmin`。Jev valid 0.97；人工：成立，改成"本环境的物理槽"
  - 修正提交 `ea0b9ac` 并推送。hugo 57 页、check-site 0 失败、unittest 通过
  - Codex 对 README 重写：没发现与代码矛盾的说法
- README 重写成 Go 版（中英双语）：从源码构建、命令表和默认阈值、连接参数、退出码、权限要求；shell 版指向 `v1.0.0`
- 两个架构都编译了（`-trimpath -s -w`，版本号取 `git describe`）：
  - linux/amd64 sha256 `2d1c39da…29365976`，在 node1 实跑
  - linux/arm64 sha256 `9dbd21ad…54be2bf0`，只在容器里跑过 `--version`/`--help`，**没有对真实 KES 跑过**
- 计划里写了 L6 验收步骤（第 0–9 步，用户本人执行）。我按步骤干跑过：`session` 不带 pid 得 64，`--port 1` 得 69，`kbdiag_ro` 跑 sessions 得 UNKNOWN、跑 status 得 0，`locks --json` 能解析。这只是检查步骤本身能跑，**不算验收**

### 等用户的门

1. 照计划里的 L6 步骤走一遍并在 chronicle 签字
2. 打 tag `v2.0.0-alpha.1`
3. kbdiag-docs `v2` 合进 main（上线 v2 手册）

### 已知缺口

- Mac 休眠会让 VM 时钟跳，VM 测试必须 `caffeinate -dimsu`
- status 在无 CONNECT 权限时库大小为 NULL，只有 L2 覆盖
- arm64 没对真实 KES 跑过
- kbdiag-docs reference 的 `_index` 仍是 v1 README 快照，发布时替换
- 还没有 release 流程和预编译二进制，tag 之前用户从源码构建

## L6 改成按场景验收，由 AI 执行（用户 2026-09-24 定）

用户看了 L6 步骤后决定：验收由我来跑，用户关心的不是实现，而是"工具满足了什么场景"。所以 L6 改成按 PRD §11 的 DS 场景逐个造现场、实跑 kbdiag，交给用户的是场景覆盖结论，由用户判断够不够用。

- 二进制：`0174e5a`，amd64，部署到 node1/node2。`internal`/`cmd`/`e2e` 自 `0a4d61e` 起没改过，所以上午两节点 e2e 全绿的结果仍然有效（DS-04、2PC、slot、只读账号、备库这几类场景靠它）
- 新造的现场（node1，06:55–06:59 +08，跑完都已清理，注入会话计数 0）：
  - DS-01 空闲实例：6 条命令都 OK/0
  - DS-02/03：用 kbdiag_ro 开 94 个连接，connections=99/100。system 用 kbdiag 仍连得上（走保留槽）；kbdiag_ro 再连得 69，报 "remaining connection slots are reserved"。**status 判 OK，不报警**；sessions 默认前几行是后台进程，另有 102 行没显示，看不出"谁占的"
  - DS-05：等锁 0.5 秒 OK，10 秒 WARN
  - DS-07：A 持 t1、B 持 t2 等 t1、C 等 t2。locks 两条 WARN，C 的建议指向 B 而不是根 A；对 B 跑 session 能看到它既被 A 挡、又挡着 C；对 A 跑 session 看到 idle in transaction 38 秒持排他锁。逐跳能找到根，不能一步到根
  - DS-09：读事务持 AccessShare，ALTER 等 AccessExclusive，后面两个查询排在 ALTER 后面。locks 都列出来了；没有 DDL 专项建议；锁列表没有 SQL，要靠 session 才知道哪个是 ALTER
  - DS-10/11：8 个 pg_sleep，`sessions --active --limit 3` 按时长排，提示"7 more rows not shown"。慢 SQL 本身没有阈值、不报警
  - DS-08：死锁被 KES 解掉后，locks/status 都看不到任何痕迹
  - DS-21：备库上 `status` 只显示角色 standby，**没有 WAL 接收状态**；而 slots 的 FAIL 建议写着"在备库上运行 kbdiag status，确认是否在接收 WAL"——建议指向的命令回答不了这个问题（`internal/rule/inst.go` 的 Slots）
- 顺带发现：README 的构建命令用 `git describe --tags --always`，现在得到 `shell-final-5-g0174e5a`，版本号带着 shell 版 tag 名，会误导
- 注入脚本小坑：`lib.sh` 的远端 helper 开着 `set -e`，场景脚本里 kbdiag 返回非 0 就中断；ksql 不读 PGPASSWORD，密码要写进 conninfo

## 场景验收后的补丁（用户 2026-09-24 批准，Codex 做对抗审查）

用户同意补场景验收发现的 3 个问题，要求对抗审查由 Codex 做。设计理由写在 CLAUDE.md "场景验收后的补丁"。

- 改动：
  - `status` 新增 `inst.connections`：已用连接占"普通用户可用数"（max_connections − superuser_reserved_connections）≥80% WARN、≥100% FAIL，`--conn-warn`/`--conn-fail` 可调（必须是正的有限数，fail 不低于 warn，否则 64）
  - slots 的 FAIL 建议从 `kbdiag status` 改成在备库跑 `kbdiag sessions`（看有没有 walreceiver）；PRD §5.1 的 slots 示例同步
  - README 构建命令改成 `git describe --tags --match 'v2*' --always`，当前得到 `8511e1d`，不再带 `shell-final`
  - 新注入脚本 `e2e/inject/conn.sh`：用 kbdiag_ro 开空闲 TCP 会话占满可用连接，tag `kbdiag_inj_conn`
  - e2e `TestStatus` 加两个子测试：`--conn-warn 1` 得 WARN/1；conn.sh 占满得 FAIL/2，且先用 ksql 确认 `kbdiag_inj_conn` 会话数 > 0
- Codex 审查 4 条，Jev 分级和人工核对：

| 意见 | Jev | 人工 | 处理 |
|---|---|---|---|
| P1 conn.sh 可能误杀同名会话、up 失败不清理 | valid 0.58（置信 0.37），严重度 2.2 | 不成立：`inject()` 在 up 前注册 Cleanup；`kbdiag_inj_` 前缀是测试注入专用命名空间，lib.sh 同样做法 | 不改；Codex 复核接受 |
| P2 e2e 只看总连接数，conn.sh 一个没开也能绿 | valid 0.77，2.2 | 部分成立（实验室不可能自然满，但断言确实不证明注入生效） | 加 ksql 计数断言 |
| P2 阈值 flag 接受 NaN/0/负数 | valid 0.67，2.7 | 成立 | 校验 + 5 个 main_test 用例 |
| P2 engineering.md §6.1 边界示例仍是 70/90% of max | valid 0.84，1.3 | 成立 | 改成可用 97 的 77/78、96/97、99 |

  Jev 在 P1 上判 valid 但置信很低，人工和 Codex 复核都认为不成立——又一次说明 Jev 只能当分诊信号。
- 验证：`go vet ./...`、`go test ./...` 172 通过；VM e2e（`caffeinate -dimsu`）node1 ok 136.9s、node2 ok 83.9s；`-run TestStatus -v` 确认两个新子测试在 node1 实际运行并通过。conn.sh 之前手工在两节点上跑过 up/down，清理后注入会话数 0
- Codex 复核（read-only）：4 条全部确认，已修 3 条正确完整，P1 不改可接受，无新 P1/P2
- 场景覆盖变化：DS-02/03 由"部分"变为"满足"（status 在连接占满前报 WARN、满了报 FAIL，建议指向 sessions 找扎堆）；DS-21 的错误建议已纠正
- 剩下的门（都要用户确认）：tag `v2.0.0-alpha.1`；kbdiag-docs `v2` 合 main
- kbdiag-docs `v2`：status、slots 中英 4 页按 6803c61 在 node1/node2 重新实跑采样（07:32 +08），status 新增"连接快用完"一节（conn.sh 注入，98/97 占 101% FAIL，exit 2）和 flag/退出码 1、2；slots 的 verify 改为 sessions。check-site 标记改为 6803c61；hugo 构建、check-site（57 页 0 失败）、unittest 通过，已推送 6e77b3e。注入已清理（conn down、slot down 均 0，槽恢复 active）

## 发布门：用户同意 tag 和 docs 合 main（2026-09-24）

- 用户回复"同意"（tag `v2.0.0-alpha.1` + kbdiag-docs `v2` 合 main）。CI 在 5222219 上 success
- 打 tag 并推送的命令被 Claude Code 的 auto mode 权限分类器拦下（Create Public Surface），命令没有执行，本地和远端都没有 v2 tag。要用户自己执行或放开权限
- docs 合 main 前核查发现：`v2` 分支上除了 reference 下 7 个命令页，其余页面仍是 v1 内容——首页、get-started（含 report-example）、features、scenarios（lock-waits、slow-sql、replication-lag）、about，以及 reference/_index（v1 README 快照：curl `dist/kbdiag`、`kbdiag all/check/update`、KB_* 环境变量，这些在 v2 都不存在）。直接合进 main 会上线一个入口页全是 v1 命令、命令页是 v2 的站点。暂停合并，等用户定这些页怎么处理

## kbdiag-docs `v2` 的 v1 页面改写（2026-09-24）

- 用户回复"改"：合 main 之前先把 `v2` 上的 v1 页面换成 v2 内容
- 改写：首页数据 `data/home/{en,zh}.yaml`（6 张卡片指向 features 各锚点），`hugo.yaml` 和 `content/_index` 的描述，docs 首页，about，features，get-started（从源码构建加 scp、连接 flag、退出码 0/1/2/3/64/69、下一步怎么查），scenarios 首页，reference/_index（改成 v2 README 快照，加 7 个命令页链接）
- 重新实跑：reading-results、lock-waits、slow-sql 中英 6 页，node1 10:20 前后，二进制 6803c61（`kbdiag --version` 输出 `kbdiag version 6803c61`）。lock-waits：435317 被 435308 挡住，WARN/1，清理后 `locks` OK/0；slow-sql：436010 跑 `pg_sleep(3600)`；reading-results：`sessions --idle-in-txn-warn 5` WARN/1，pid 436492。注入脚本 down 之后复查，0 个残留
- 删除：get-started/report-example、scenarios/replication-lag（v2 没有 report 命令，也没有复制延迟命令），以及配套的 `static/samples/inspection-report-20260916.md` 和 `sample-report` shortcode
- slow-sql 页写明 v2 还没有慢 SQL 阈值：长语句只列出和排序，本身不报 WARN
- check-site：CORE 删掉两页，三页的标记换成 6803c61 加 PID；删掉报告样例的校验和不再用到的 `import re`
- 中途发现 `hugo.yaml` 英文描述里有未加引号的冒号，hugo 报配置错误，只是 `--quiet` 下没输出、public 也没生成；加了引号。之后 hugo 构建，check-site 53 页、2855 次链接检查、0 失败，unittest 7 个通过；构建产物里查不到 report-example、replication-lag、inspection-report
- Codex 审查（read-only，只看事实准确性）报 2 条 medium，对照 `internal/rule/rule.go` 的 `verdictOf` 都成立：about 页"没采到就是 UNKNOWN"说得太宽（status 看不到的库大小不参与判定），以及 README 和手册首页的权限段落漏了"WARN/FAIL 优先于 UNKNOWN"。已修改 about 中英两页、kbdiag README 中英两段，并重新生成手册首页快照
- kbdiag-docs `v2` 已提交并推送 ce5adca
- 合 main 被 auto mode 分类器拦下（Production Deploy），命令没有执行，docs main 仍是 v1。tag `v2.0.0-alpha.1` 也还没打（之前被拦的那次）。这两步都等用户自己执行或放开权限；建议先推 tag，再合 docs main

## v0.1 发布（2026-09-24）

- 用户说明工作方式：正确性由 AI 兜底，只看结果；被权限分类器拦时，AI 发一份写明命令、影响、风险和回退办法的授权申请，用户看过就放行
- 申请前核实：kbdiag main bc0945e 的 CI 已通过，远端没有 v2* tag；kbdiag-docs main 没有 `v2` 之外的提交，`v2` 比 main 多 5 个提交，`git merge-tree` 试合并 0 冲突
- 用户授权后执行：
  - tag `v2.0.0-alpha.1`（附注 tag，指向 bc0945e）已推送，`git ls-remote` 确认存在
  - kbdiag-docs：`v2` 以 `--no-ff` 合进 main（eb5a078），已推送。Deploy to GitHub Pages 成功；Deploy to Cloudflare Pages 显示 skipped，因为仓库变量 `CLOUDFLARE_PAGES_ENABLED` 没开，最近 4 次都是 skipped，属于正常
  - 线上抽查：首页、get-started、lock-waits、zh 首页、zh reference/locks 都返回 200，内容是 v2（含 6803c61 或 0a4d61e 的采样）；report-example 返回 404（已删除）。zh locks 第一次 curl 返回 000，重试得 200，判断是网络抖动
- 计划的验收标准 G1–G7 都已满足（证据见本文件各节；G7 按用户决定改成由 AI 做场景验收）。按防僵尸规则，计划标记 done：v0.1 范围的理由写进 CLAUDE.md "v0.1 范围"一节，计划文件删除。CLAUDE.md 里 kbdiag-docs 的分支规则改成"每次发布时把 `v2` 合进 main"
- 下一步：没有进行中的阶段。v0.2 的范围（locks --tree、慢 SQL 阈值、复制延迟、repmgr 状态、场景族命名等）要等用户开新计划
