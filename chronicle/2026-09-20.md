# 2026-09-20

## 会话摘要：从"没方向"到"项目被判定失败"，产出两份文档

### 1. 重启中断的 ora/pg_profile 能力缺口调研

用户开场说"工具现在没有开发方向了"。没有直接 autopilot 硬造方向，先核实项目状态（2026-07-22 暂停条款仍在生效，无 open issue），问清楚后用户选择重启中断的 `docs/superpowers/specs/2026-07-25-capability-gap-research.md` 调研线程。

核实发现清单第 1/2 项（`sys_kwr`/`track_real_stats` 探测）其实已经被 `workload` 命令交付，不是真断头。上 kes-node1（VM 之前是停机状态，冷启动后核实）：

- `track_real_stats` 是 `superuser` 级别可 `SET`，不需要重载/重启（好于文档原先估计）
- `sys_kwr.enable` 当前是 off（VM 重建后被重置）
- **关键新发现**：KingbaseES 自带 `sys_hypo` 扩展（v1.1.2，"Hypothetical advisor"），函数集与 HypoPG 一一对应，已冒烟测试跑通全链路（建假设索引 → EXPLAIN 命中 → list → reset）。解除了候选 3"HypoPG 式 what-if 索引顾问"唯一的阻塞未知项。

调研文档更新提交（commit `cb39df1`）。用户选定候选 3 为下一步方向，**尚未开始写需求文档**。

### 2. 用户中途给出严厉评价："这个工具我认为是失败的"

四条指控：命令功能重叠、每个命令没有使用场景说明、文档没有例子、没有每个命令的测试用例，需求/验收/测试报告都不满意。

没有辩护，逐条核实：

- **命令重叠**：穷举全部 35 个命令的子命令 dispatch，发现 4 组真实重叠——膨胀（`perf bloat`/`idx bloat`/`space frag`）、索引健康（`idx`/`perf index`/`advisor index`）、vacuum（`perf vacuum`/`advisor vacuum`）、等待事件（`wait`/`perf wait`/`locks wait`），波及 6 个命令。
- **文档没场景/没例子**：README 命令表 35 行清一色一行描述，零示例、零"何时用 A 不用 B"。
- **测试质量**：发现用户自己此前已经写过 `docs/dba-scenario-acceptance.md`（638 行未提交）——24 个故障场景 + 8 个能力缺口（GAP-1~8），质量很高，卡在"场景设计完成，没有变成可执行测试"这一步。本轮直接引用，未重复劳动。

产出 `docs/superpowers/specs/2026-09-20-command-overlap-and-docs-audit.md`（commit `7b172f0`），结论：四条指控都成立，根因是同一个——项目从头到尾是"一个命令一个 tracer-bullet 交付"，从未做过全局收敛（命令表、文档结构、测试策略）。给出三条后续路径（命令整合/测试重建/文档重写），不预设选哪条，留给用户下次拍板。

### 未完成，留给下次

- HypoPG what-if 索引顾问：候选已确认可行，需求文档还没写
- 三条整合路径都没开始动手，用户要"更完整的诊断报告再决定"，本次的两份审计文档就是这个报告，下次应该先问用户看完后选哪条路径
- `dba-scenario-acceptance.md`、`docs-site-setup.md`、`problem-signal-extraction-2026-09-08.md`、`scripts/init-docs.py` 目前仍是未提交状态（本次审计文档引用了 dba-scenario-acceptance.md 但没有提交它本身——是否要提交需要用户确认，不应该顺手带进无关 commit）
