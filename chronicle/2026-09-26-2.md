# 2026-09-26（续）

## 阶段 9 拍板：1–9 同意，第 10 条选 A

用户原话："第10条选A，其余按建议实现的都同意"。

- 1–8 已按建议实现，CLAUDE.md 各节的"待用户确认"改成"用户 2026-09-26 确认"；第 9 条（暂停的 walreceiver 是否按 `wal_receiver_timeout` 判）仍未决定，没动。
- 第 10 条 A：`wait.summary` 加一列 `background`（组内没被遮蔽的后台进程 pid，排除 client backend 和 parallel worker），`facts.Wait.Background` 不进 `Row()`，JSON 不变。文本在"active 且没有等待事件"的组里把这些 pid 移进 `background` 计数；后台进程卡在真正等待上的照样列出。

### 验证（实跑）

- L1/L2：`go vet ./...` 通过；`go test ./...` 302 个通过。新增 `TestWaitsKshRunning`（用 `stage9/waits_node1_lock` 复原 3279 在跑的那一刻）和 `TestWaitsTextEdges` 的两行；去掉 report 的改动后两个测试都失败（变异检查）。
- L3：新 SQL 在 node1、node2 上分别用 system 和 kbdiag_ro 跑过：Activity 组和备库的 startup/walreceiver/bgwriter/checkpointer 组有 pid，客户端组为空，遮蔽组为 NULL（kbdiag_ro 下 node1 遮蔽 10 个、node2 遮蔽 5 个）。KES 支持聚合的 `FILTER`。
- L4：两节点 `TestWaits`、`TestWaitsTrackActivitiesOff`、`TestNonMonitorUser` 通过；node2 上 `with_sys_monitor_nothing_is_redacted` 是预期 SKIP。e2e 新断言后台 pid 不出现在 `(running)` 行；ksh writer 被赶上的竞态很少碰上，e2e 通过不能说明碰上过，这一条的正向证据只有 L2 的复原。
- 改动：`internal/{facts,probe,report}/wait.go|waits.go`、`internal/scenario/commands_test.go` 和两个 golden、`e2e/commands_test.go`（commit `9a05ffb`）；CLAUDE.md waits 节、`docs/queries.md` E1、PRD waits 行、计划 §5。

### 没做（门槛）

- 没合 main、没推 main、没打 tag。
- kbdiag-docs 各命令页没写：等用户确认输出。
- 没为 `inst.upstream` 写注入脚本。
