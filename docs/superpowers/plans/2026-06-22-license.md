# License 授权检查 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `kbdiag license` 命令查询 KingbaseES 授权有效期和正式/试用状态，并在 `kbdiag status` 加一行摘要。

**Architecture:** 新建 `lib/cmd_license.sh` 实现 `cmd_license()`，调用 `get_license_validdays()` 判断状态，调用 `get_license_info()` 解析摘要字段。`cmd_status()` 末尾追加轻量 validdays 检查。`build.sh` 的 lib 列表、dispatch case、USAGE heredoc 同步更新，`kbdiag.sh` 的 source 列表和 dispatch 同步更新，`build.sh` 重新打包生成 `dist/kbdiag`。

**Tech Stack:** Bash, KingbaseES SQL 函数 (`get_license_validdays`, `get_license_info`)

## Global Constraints

- 纯 Shell，无外部依赖（`grep`/`sed`/`tr` 已存在于目标系统）
- 所有 SQL 通过 `ksql_q()` 执行（见 `lib/core.sh:139`）
- KingbaseES 布尔返回 `true`/`false`，需 `tr -d '[:space:]'` 去空白
- 输出函数：`ok()` / `warn()` / `fail()` / `info()` / `hdr()`（`lib/core.sh:56-72`）
- JSON 输出：`json_begin` / `json_item` / `json_end`（`lib/core.sh:99-129`）
- `VERBOSE` / `QUIET` / `OUTPUT_FMT` 已在 `core.sh` 声明为全局变量
- 运行环境：`kingbase` OS 用户，DB port 54321
- 测试通过 SSH 编排 VM（见 `test/lib/ssh_helpers.sh`）

---

### Task 1: 新增阈值 + 新建 `cmd_license.sh` + 测试用例

**Files:**
- Modify: `lib/core.sh`（在第 35 行 `KB_FAIL_OLDEST_TXN` 之后追加一行）
- Create: `lib/cmd_license.sh`
- Create: `test/cases/test_license.sh`

**Interfaces:**
- Produces: `cmd_license()` — 无参数，读取全局 `OUTPUT_FMT` / `VERBOSE` / `QUIET` / `KB_WARN_LICENSE_DAYS`

- [ ] **Step 1: 在 `lib/core.sh` 追加阈值**

在 `lib/core.sh` 第 35 行（`KB_FAIL_OLDEST_TXN` 行）之后插入：

```bash
KB_WARN_LICENSE_DAYS="${KB_WARN_LICENSE_DAYS:-30}"
```

最终该区块末尾如下：
```bash
KB_WARN_OLDEST_TXN="${KB_WARN_OLDEST_TXN:-3600}"   # 1h in seconds
KB_FAIL_OLDEST_TXN="${KB_FAIL_OLDEST_TXN:-14400}"  # 4h in seconds
KB_WARN_LICENSE_DAYS="${KB_WARN_LICENSE_DAYS:-30}"
```

- [ ] **Step 2: 创建 `lib/cmd_license.sh`**

```bash
cmd_license() {
  [[ "$OUTPUT_FMT" == "json" ]] && json_begin "license"
  hdr "License"

  local validdays
  validdays=$(ksql_q "SELECT get_license_validdays();" | tr -d '[:space:]')
  validdays="${validdays:-0}"

  local status_label status_level
  if [[ "$validdays" == "-2" ]]; then
    status_label="永久授权（正式）"
    status_level="ok"
  elif [[ "$validdays" -gt "$KB_WARN_LICENSE_DAYS" ]]; then
    status_label="剩余 ${validdays} 天"
    status_level="ok"
  elif [[ "$validdays" -gt 0 ]]; then
    status_label="剩余 ${validdays} 天，即将到期"
    status_level="warn"
  else
    status_label="已过期（validdays=${validdays}）"
    status_level="fail"
  fi

  local info_text serial product edition username project issued
  info_text=$(ksql_q "SELECT get_license_info();")
  serial=$(echo "$info_text"   | grep 'License序列号'  | sed 's/.*--- 启用 --- //' | tr -d ' \t\r\n+')
  product=$(echo "$info_text"  | grep '产品名称'       | sed 's/.*--- 启用 --- //' | tr -d ' \t\r\n+')
  edition=$(echo "$info_text"  | grep '细分版本模板名' | sed 's/.*--- 启用 --- //' | tr -d ' \t\r\n+')
  username=$(echo "$info_text" | grep '用户名称'       | sed 's/.*--- 启用 --- //' | tr -d ' \t\r\n+')
  project=$(echo "$info_text"  | grep '项目名称'       | sed 's/.*--- 启用 --- //' | tr -d ' \t\r\n+')
  issued=$(echo "$info_text"   | grep '生产日期'       | sed 's/.*--- 启用 --- //' | tr -d ' \t\r\n+')

  if [[ "$OUTPUT_FMT" == "json" ]]; then
    json_item "license_status" "$status_level" "$status_label"  "validdays=${validdays}"
    json_item "serial"         "ok"            "$serial"        ""
    json_item "product"        "ok"            "$product"       ""
    json_item "edition"        "ok"            "$edition"       ""
    json_item "user"           "ok"            "$username"      ""
    json_item "project"        "ok"            "$project"       ""
    json_item "issued"         "ok"            "$issued"        ""
    json_end
    return
  fi

  case "$status_level" in
    ok)   ok   "授权状态: $status_label" ;;
    warn) warn "授权状态: $status_label" ;;
    fail) fail "授权状态: $status_label" ;;
  esac

  info "序列号:   $serial"
  info "产品名称: $product"
  info "版本模板: $edition"
  info "用户名称: $username"
  info "项目名称: $project"
  info "生产日期: $issued"

  if [[ -n "$VERBOSE" ]]; then
    echo ""
    echo "$info_text"
  fi
}
```

- [ ] **Step 3: 创建 `test/cases/test_license.sh`**

```bash
test_license_exit_zero() {
  local code; code=$(ssh_node1_exit "$KBDIAG_REMOTE license")
  assert_exit_code 0 "$code"
}

test_license_status_ok() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE license")
  assert_contains "$out" "[OK]"
  assert_contains "$out" "授权状态"
}

test_license_contains_serial() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE license")
  assert_contains "$out" "序列号"
}

test_license_contains_product() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE license")
  assert_contains "$out" "产品名称"
}

test_license_contains_edition() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE license")
  assert_contains "$out" "版本模板"
}

test_license_quiet_hides_info() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE -q license")
  assert_not_contains "$out" "[INFO]"
}

test_license_json_valid() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE --format json license")
  assert_json_valid "$out"
}

test_license_json_contains_status() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE --format json license")
  assert_contains "$out" "license_status"
}
```

- [ ] **Step 4: 验证文件存在**

```bash
ls lib/cmd_license.sh test/cases/test_license.sh
```

期望：两个文件均列出，无报错。

- [ ] **Step 5: Commit**

```bash
git add lib/core.sh lib/cmd_license.sh test/cases/test_license.sh
git commit -m "feat: add cmd_license — validdays + info field parsing"
```

---

### Task 2: 集成到 dispatch、status、build

**Files:**
- Modify: `lib/cmd_status.sh`（末尾追加 license 摘要）
- Modify: `kbdiag.sh`（source 列表 + dispatch）
- Modify: `build.sh`（lib 列表 + dispatch + USAGE help 文本）
- Rebuild: `dist/kbdiag`（`bash build.sh` 生成）

**Interfaces:**
- Consumes: `cmd_license()` from Task 1; `KB_WARN_LICENSE_DAYS` from Task 1

- [ ] **Step 1: `lib/cmd_status.sh` 追加 license 摘要行**

将 `lib/cmd_status.sh` 的整个文件内容替换为：

```bash
cmd_status() {
  hdr "Instance status"

  local pid
  pid=$(pgrep -U kingbase -f "kingbase.*-D $KB_DATA_DIR" | head -1 || true)
  if [[ -n "$pid" ]]; then
    ok "kingbase process running (pid $pid)"
  else
    fail "kingbase process not found"
    return 1
  fi

  local ver
  if ver=$(ksql_q "SELECT version();" 2>&1); then
    ok "DB connectable — $ver"
  else
    fail "Cannot connect to DB on port $KB_PORT"
    return 1
  fi

  local is_standby
  is_standby=$(ksql_q "SELECT pg_is_in_recovery()::text;" | tr -d '[:space:]')
  if [[ "$is_standby" == "false" ]]; then
    ok "Role: PRIMARY"
  else
    ok "Role: STANDBY"
  fi

  local uptime
  uptime=$(ksql_q "SELECT date_trunc('second', now() - pg_postmaster_start_time())::text;")
  info "Uptime: $uptime"

  local lic_days
  lic_days=$(ksql_q "SELECT get_license_validdays();" | tr -d '[:space:]')
  lic_days="${lic_days:-0}"
  if [[ "$lic_days" == "-2" ]]; then
    ok "License: 永久授权（正式）"
  elif [[ "$lic_days" -gt "$KB_WARN_LICENSE_DAYS" ]]; then
    ok "License: 剩余 ${lic_days} 天"
  elif [[ "$lic_days" -gt 0 ]]; then
    warn "License: 剩余 ${lic_days} 天到期"
  else
    fail "License: 已过期"
  fi
}
```

- [ ] **Step 2: `kbdiag.sh` — source 列表末尾追加 `cmd_license.sh`**

将第 32 行：
```bash
  "$SCRIPT_DIR"/lib/cmd_advisor.sh; do
```
改为：
```bash
  "$SCRIPT_DIR"/lib/cmd_advisor.sh \
  "$SCRIPT_DIR"/lib/cmd_license.sh; do
```

- [ ] **Step 3: `kbdiag.sh` — dispatch 追加 `license`**

将 `advisor)` 行：
```bash
  advisor)     cmd_advisor "$SUBCMD" "${CMD_ARGS[@]+"${CMD_ARGS[@]}"}" || exit $? ;;
```
改为：
```bash
  advisor)     cmd_advisor "$SUBCMD" "${CMD_ARGS[@]+"${CMD_ARGS[@]}"}" || exit $? ;;
  license)     cmd_license ;;
```

- [ ] **Step 4: `build.sh` — lib 文件列表追加 `cmd_license.sh`**

将第 22 行：
```bash
            lib/cmd_advisor.sh; do
```
改为：
```bash
            lib/cmd_advisor.sh \
            lib/cmd_license.sh; do
```

- [ ] **Step 5: `build.sh` — dispatch 追加 `license`**

将 `advisor)` 行（第 65 行）：
```bash
  advisor)     cmd_advisor "$SUBCMD" "${CMD_ARGS[@]+"${CMD_ARGS[@]}"}" || exit $? ;;
```
改为：
```bash
  advisor)     cmd_advisor "$SUBCMD" "${CMD_ARGS[@]+"${CMD_ARGS[@]}"}" || exit $? ;;
  license)     cmd_license ;;
```

- [ ] **Step 6: `build.sh` — USAGE heredoc 追加 help 行**

在 USAGE heredoc 的 `[OPS]` 区块中，将：
```
  status              Instance process, connectivity, role, uptime
  cluster             Repmgr cluster topology
```
改为：
```
  status              Instance process, connectivity, role, uptime
  license             License 授权状态（有效期、正式/试用）
  cluster             Repmgr cluster topology
```

- [ ] **Step 7: 运行 `build.sh` 重新打包**

```bash
bash build.sh
```

期望：输出 `Built: dist/kbdiag`，无报错。

- [ ] **Step 8: 语法检查**

```bash
bash -n dist/kbdiag && echo "syntax OK"
```

期望：输出 `syntax OK`。

- [ ] **Step 9: Commit**

```bash
git add lib/cmd_status.sh kbdiag.sh build.sh dist/kbdiag
git commit -m "feat: wire license into dispatch, status summary, help, and build"
```

---

### Task 3: 验收测试

**Files:**
- Run: `bash test/run_tests.sh license`
- Run: `bash test/run_tests.sh status`

**Interfaces:**
- Consumes: 已部署的 `dist/kbdiag`（Task 2 build 产物）

- [ ] **Step 1: 运行 license 测试**

```bash
bash test/run_tests.sh license
```

期望输出（8 个测试全部 PASS）：
```
==> Deploying dist/kbdiag to both nodes...
==> test/cases/test_license.sh
  [PASS] test_license_exit_zero
  [PASS] test_license_status_ok
  [PASS] test_license_contains_serial
  [PASS] test_license_contains_product
  [PASS] test_license_contains_edition
  [PASS] test_license_quiet_hides_info
  [PASS] test_license_json_valid
  [PASS] test_license_json_contains_status

Total: 8 passed, 0 failed / 8 run
```

- [ ] **Step 2: 运行 status 回归测试**

```bash
bash test/run_tests.sh status
```

期望：所有原有 status 测试均 PASS（测试文件未改动，应全 PASS）。

- [ ] **Step 3: 手动验证 `kbdiag license` 输出**

```bash
ssh -p 57103 kevin@127.0.0.1 "sudo -i -u kingbase bash -c '/tmp/kbdiag license'"
```

期望：看到 `[OK]    授权状态: 永久授权（正式）` 及序列号、用户名称、版本模板等字段。

- [ ] **Step 4: 手动验证 `kbdiag status` 包含 license 行**

```bash
ssh -p 57103 kevin@127.0.0.1 "sudo -i -u kingbase bash -c '/tmp/kbdiag status'"
```

期望：输出末尾包含 `[OK]    License: 永久授权（正式）`。

- [ ] **Step 5: 验证 help 文本**

```bash
ssh -p 57103 kevin@127.0.0.1 "sudo -i -u kingbase bash -c '/tmp/kbdiag --help'" | grep license
```

期望：输出包含 `license` 和 `License 授权状态`。

- [ ] **Step 6: 如有失败修复后重跑 Step 1-2，必要时追加 commit**

```bash
git add -p
git commit -m "fix: license corrections after test run"
```
