# 功能层 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 扩展两个薄弱命令：`watch` 支持 `kbdiag watch [subcmd] [interval]` 自然接口并默认为 status；`stat` 新增 5 个 AWR 维度（Top SQL、等待事件、锁计数、临时文件数、checkpoint 频率）。

**Architecture:** `watch` 只改参数解析逻辑（4 行），其余 case 分支已有；`stat` 每个新维度写成独立内部函数 `_stat_section_*`，在 `cmd_stat` 末尾顺序调用，保持文件可读。所有改动在 `lib/` 下完成，`bash build.sh` 重建 `dist/kbdiag`。

**Tech Stack:** Pure Bash, ksql, awk, SSH Lima VM

## Global Constraints

- 改完 `lib/*.sh` 必须 `bash build.sh` 重建 `dist/kbdiag`
- 测试使用 SSH 助手：`ssh_node1`（输出）、`ssh_node1_exit`（退出码）
- `stat` 新增节在 sys_stat_statements 不可用时静默跳过（`2>/dev/null || true`）
- watch 兼容旧接口 `kbdiag watch 5 sessions`（interval 在前）
- 改完后 `shellcheck lib/cmd_watch.sh lib/cmd_stat.sh` 必须零错误
- 提交信息英文

---

### Task 1: watch 参数解析修复 + 默认子命令

**Files:**
- Modify: `lib/cmd_watch.sh`（第 1-4 行参数解析）
- Modify: `dist/kbdiag`（rebuild 产物）
- Modify: `test/cases/test_watch.sh`（追加 2 个测试）

**Interfaces:**
- Produces: `cmd_watch()` 接受 `watch [subcmd] [interval]` 或 `watch [interval] [subcmd]` 两种顺序；无参数默认为 `watch status 5`

- [ ] **Step 1: 写失败测试（当前自然接口会报 unknown command）**

在 `test/cases/test_watch.sh` 末尾追加：

```bash
test_watch_natural_interface_sessions() {
  # kbdiag watch sessions（不传 interval）应能运行，不报 "unknown command"
  local out; out=$(ssh_node1 "timeout 3 $KBDIAG_REMOTE watch sessions 2>&1 || true")
  assert_not_contains "$out" "unknown command"
  assert_not_contains "$out" ": line "
}

test_watch_default_no_args_runs_status() {
  # kbdiag watch（无参数）应默认运行 status，不报 "unknown command ''"
  local out; out=$(ssh_node1 "timeout 3 $KBDIAG_REMOTE watch 2>&1 || true")
  assert_not_contains "$out" "unknown command ''"
  assert_not_contains "$out" ": line "
}
```

- [ ] **Step 2: 确认测试当前失败**

```bash
bash test/run_tests.sh watch
```
预期：`test_watch_natural_interface_sessions` 和 `test_watch_default_no_args_runs_status` FAIL（unknown command）。

- [ ] **Step 3: 修改 `lib/cmd_watch.sh` 参数解析**

将 `lib/cmd_watch.sh` 第 1-4 行替换为以下内容（其余行不变）：

```bash
cmd_watch() {
  local interval=5
  local subcmd="status"
  local args=()

  while [[ $# -gt 0 ]]; do
    case "$1" in
      status|sessions|wait|check|locks) subcmd="$1" ;;
      perf) subcmd="$1"; shift; args=("$@"); break ;;
      ''|*[!0-9]*) ;;
      *) interval="$1" ;;
    esac
    shift
  done
```

完整 `lib/cmd_watch.sh` 改后为：

```bash
cmd_watch() {
  local interval=5
  local subcmd="status"
  local args=()

  while [[ $# -gt 0 ]]; do
    case "$1" in
      status|sessions|wait|check|locks) subcmd="$1" ;;
      perf) subcmd="$1"; shift; args=("$@"); break ;;
      ''|*[!0-9]*) ;;
      *) interval="$1" ;;
    esac
    shift
  done

  while true; do
    clear 2>/dev/null || printf '\033[2J\033[H'
    echo "=== kbdiag watch — $(date) — every ${interval}s (Ctrl+C to stop) ==="
    case "$subcmd" in
      status)      cmd_status ;;
      sessions)    cmd_sessions ;;
      wait)        cmd_wait ;;
      check)       cmd_check || true ;;
      perf)        cmd_perf "${args[0]:-}" ;;
      locks)       cmd_locks "${args[0]:-}" ;;
      *)           echo "watch: unknown command '$subcmd'" >&2; return 1 ;;
    esac
    sleep "$interval"
  done
}
```

- [ ] **Step 4: shellcheck**

```bash
shellcheck lib/cmd_watch.sh
```
预期：零错误、零警告（若有 SC2034 等误报，加行内注解抑制）。

- [ ] **Step 5: 重建**

```bash
bash build.sh
```
预期：`Built: dist/kbdiag`

- [ ] **Step 6: 运行测试**

```bash
bash test/run_tests.sh watch
```
预期：全部 8 个 PASS（原 6 个 + 新增 2 个），0 FAIL。

- [ ] **Step 7: Commit**

```bash
git add lib/cmd_watch.sh dist/kbdiag test/cases/test_watch.sh
git commit -m "feat: fix watch arg parsing — support natural 'watch sessions' interface with status default"
```

---

### Task 2: stat AWR 扩展 — 等待事件 + 锁计数 + 临时文件数

**Files:**
- Modify: `lib/cmd_stat.sh`
- Modify: `dist/kbdiag`（rebuild 产物）
- Modify: `test/cases/test_stat.sh`（追加 3 个测试）

**Interfaces:**
- Produces: `cmd_stat` 在现有 5 指标后追加三个新段：等待事件 delta、锁获取 delta、临时文件数 delta

- [ ] **Step 1: 写失败测试**

在 `test/cases/test_stat.sh` 末尾追加：

```bash
test_stat_shows_wait_events() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stat --interval=1")
  if echo "$out" | grep -qiE 'Wait event|wait_event|等待'; then
    _pass
  else
    _fail "stat missing wait events section: $(echo "$out" | tail -5)"
  fi
}

test_stat_shows_temp_files() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stat --interval=1")
  if echo "$out" | grep -qiE 'Temp files|temp_files'; then
    _pass
  else
    _fail "stat missing temp files section: $(echo "$out" | tail -5)"
  fi
}

test_stat_shows_checkpoints() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stat --interval=1")
  if echo "$out" | grep -qiE 'Checkpoint|checkpoint'; then
    _pass
  else
    _fail "stat missing checkpoint section: $(echo "$out" | tail -5)"
  fi
}
```

- [ ] **Step 2: 确认测试当前失败**

```bash
bash test/run_tests.sh stat
```
预期：3 个新测试 FAIL（stat 目前无这些段）。

- [ ] **Step 3: 在 `lib/cmd_stat.sh` 中添加三个内部函数和主函数调用**

在 `cmd_stat()` 最后一个 `}` 之前（即 `i=$(( i + 1 ))` 行之后）追加三个内部函数，并在 `cmd_stat` 末尾调用它们。

完整 `lib/cmd_stat.sh` 改后为：

```bash
cmd_stat() {
  local interval=5 count=1
  local i=0

  # Parse arguments (skip empty strings from dispatch)
  while [[ $# -gt 0 ]]; do
    case "$1" in
      --interval=*) interval="${1#--interval=}" ;;
      --interval)   shift; interval="${1:-5}" ;;
      --count=*)    count="${1#--count=}" ;;
      --count)      shift; count="${1:-1}" ;;
      "")           ;; # skip empty arg from dispatch
    esac
    shift
  done

  hdr "Instance throughput (${interval}s sample)"

  while [[ $i -lt $count ]]; do
    local snap1 snap2
    snap1=$(ksql_q "SELECT xact_commit, xact_rollback, blks_read, blks_hit, temp_bytes, deadlocks FROM sys_stat_database WHERE datname=current_database();")
    sleep "$interval"
    snap2=$(ksql_q "SELECT xact_commit, xact_rollback, blks_read, blks_hit, temp_bytes, deadlocks FROM sys_stat_database WHERE datname=current_database();")

    printf "%s\n%s\n" "$snap1" "$snap2" | awk -F'|' -v interval="$interval" '
      NR==1 { c1=$1; r1=$2; br1=$3; bh1=$4; t1=$5; d1=$6 }
      NR==2 { c2=$1; r2=$2; br2=$3; bh2=$4; t2=$5; d2=$6
        printf "TPS commits/s:   %.1f\n", (c2-c1)/interval
        printf "TPS rollbacks/s: %.1f\n", (r2-r1)/interval
        total_blk = (bh2-bh1) + (br2-br1)
        hit_pct = (total_blk > 0) ? 100.0*(bh2-bh1)/total_blk : 100
        printf "Buffer hit rate: %.1f%%\n", hit_pct
        printf "Temp bytes/s:    %.0f\n", (t2-t1)/interval
        printf "Deadlocks/s:     %.2f\n", (d2-d1)/interval
      }'

    i=$(( i + 1 ))
    [[ $i -lt $count ]] && sleep 0 || true
  done

  _stat_section_wait_events "$interval"
  _stat_section_temp_files "$interval"
  _stat_section_checkpoints "$interval"
}

_stat_section_wait_events() {
  local interval="${1:-5}"
  printf "\nWait events (top 5 during %ss sample):\n" "$interval"
  printf "%-20s %-20s %s\n" "wait_event_type" "wait_event" "count"
  printf "%-20s %-20s %s\n" "--------------------" "--------------------" "-----"
  ksql_q "SELECT wait_event_type, wait_event, count(*) AS cnt
    FROM sys_stat_activity
    WHERE wait_event IS NOT NULL AND state != 'idle'
    GROUP BY wait_event_type, wait_event
    ORDER BY cnt DESC
    LIMIT 5;" 2>/dev/null \
  | while IFS='|' read -r wtype wevent cnt; do
      printf "%-20s %-20s %s\n" \
        "${wtype// /}" "${wevent// /}" "${cnt// /}"
    done || true
}

_stat_section_temp_files() {
  local interval="${1:-5}"
  printf "\nTemp files (${interval}s delta):\n"
  local s1 s2
  s1=$(ksql_q "SELECT temp_files, temp_bytes FROM sys_stat_database WHERE datname=current_database();" 2>/dev/null)
  sleep "$interval"
  s2=$(ksql_q "SELECT temp_files, temp_bytes FROM sys_stat_database WHERE datname=current_database();" 2>/dev/null)
  printf "%s\n%s\n" "$s1" "$s2" | awk -F'|' -v interval="$interval" '
    NR==1 { f1=$1; b1=$2 }
    NR==2 { f2=$1; b2=$2
      printf "Temp files/s:    %.2f\n", (f2-f1)/interval
      printf "Temp bytes/s:    %.0f\n", (b2-b1)/interval
    }' || true
}

_stat_section_checkpoints() {
  printf "\nCheckpoint activity:\n"
  ksql_q "SELECT checkpoints_timed, checkpoints_req,
    round(checkpoint_write_time/1000.0, 1) AS write_s,
    round(checkpoint_sync_time/1000.0, 1)  AS sync_s,
    buffers_checkpoint
    FROM sys_stat_bgwriter;" 2>/dev/null \
  | while IFS='|' read -r timed req write_s sync_s bufs; do
      printf "Checkpoints timed/req: %s / %s\n" \
        "${timed// /}" "${req// /}"
      printf "Write time (s):        %s\n" "${write_s// /}"
      printf "Sync time (s):         %s\n" "${sync_s// /}"
      printf "Buffers written:       %s\n" "${bufs// /}"
    done || true
}
```

- [ ] **Step 4: shellcheck**

```bash
shellcheck lib/cmd_stat.sh
```
预期：零错误。

- [ ] **Step 5: 重建**

```bash
bash build.sh
```
预期：`Built: dist/kbdiag`

- [ ] **Step 6: 运行测试**

```bash
bash test/run_tests.sh stat
```
预期：原有测试全部通过，新增 3 个 PASS，0 FAIL。

- [ ] **Step 7: Commit**

```bash
git add lib/cmd_stat.sh dist/kbdiag test/cases/test_stat.sh
git commit -m "feat: expand stat with wait events, temp files delta, and checkpoint sections"
```

---

### Task 3: stat AWR 扩展 — Top SQL

**Files:**
- Modify: `lib/cmd_stat.sh`（追加 `_stat_section_top_sql` 函数）
- Modify: `dist/kbdiag`（rebuild 产物）
- Modify: `test/cases/test_stat.sh`（追加 1 个测试）

**Interfaces:**
- Consumes: `sys_stat_statements` 扩展（不可用时静默跳过）
- Produces: `stat` 输出末尾附加 Top 5 SQL by total_time 段

- [ ] **Step 1: 写测试**

在 `test/cases/test_stat.sh` 末尾追加：

```bash
test_stat_top_sql_section_or_skip() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stat --interval=1")
  # 若 sys_stat_statements 可用，输出包含 Top SQL；否则输出包含跳过提示或直接无此段
  # 两种情况都合法——重要的是不崩溃
  assert_not_contains "$out" ": line "
  assert_not_contains "$out" "unbound variable"
  _pass
}
```

- [ ] **Step 2: 在 `lib/cmd_stat.sh` 中追加 `_stat_section_top_sql` 函数**

在 `_stat_section_checkpoints` 函数之后追加：

```bash
_stat_section_top_sql() {
  local avail
  avail=$(ksql_q "SELECT count(*) FROM sys_catalog.sys_class WHERE relname='sys_stat_statements';" 2>/dev/null | tr -d '[:space:]')
  [[ "${avail:-0}" -eq 0 ]] && return 0  # extension not loaded, skip silently

  printf "\nTop SQL by total time (top 5):\n"
  printf "%-8s %-10s %-10s %s\n" "calls" "total_ms" "mean_ms" "query"
  printf "%-8s %-10s %-10s %s\n" "--------" "----------" "----------" "--------------------"
  ksql_q "SELECT calls,
    round(total_exec_time::numeric, 1) AS total_ms,
    round(mean_exec_time::numeric, 1)  AS mean_ms,
    left(query, 60) AS query
    FROM sys_stat_statements
    ORDER BY total_exec_time DESC
    LIMIT 5;" 2>/dev/null \
  | while IFS='|' read -r calls total mean q; do
      printf "%-8s %-10s %-10s %s\n" \
        "${calls// /}" "${total// /}" "${mean// /}" "${q// /}"
    done || true
}
```

并在 `cmd_stat` 末尾 `_stat_section_checkpoints` 调用之后添加：

```bash
  _stat_section_top_sql
```

- [ ] **Step 3: shellcheck**

```bash
shellcheck lib/cmd_stat.sh
```
预期：零错误。

- [ ] **Step 4: 重建并运行测试**

```bash
bash build.sh && bash test/run_tests.sh stat
```
预期：全部 PASS，0 FAIL。

- [ ] **Step 5: 全量测试**

```bash
bash test/run_tests.sh
```
预期：全部通过，0 FAIL。

- [ ] **Step 6: Commit 并 push**

```bash
git add lib/cmd_stat.sh dist/kbdiag test/cases/test_stat.sh
git commit -m "feat: add Top SQL section to stat (skips gracefully if sys_stat_statements absent)"
git push
```
