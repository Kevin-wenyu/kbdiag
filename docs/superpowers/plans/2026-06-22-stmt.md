# stmt 命令 + diagnose 集成 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `kbdiag stmt` 查层命令（AWR 风格多维 Top N + queryid 下钻），并在 `diagnose` 断层增加历史均值关联（_diag_slow_queries 增强）和全量负担 Top 3（_diag_stmt_top，--full 模式）。

**Architecture:** Pure shell。`lib/cmd_stmt.sh` 实现独立 stmt 命令；`lib/cmd_diagnose.sh` 增强两个子函数。`build.sh` 把 cmd_stmt.sh 拼入 dist/kbdiag。测试通过 SSH 在 Lima VM node1 上运行。

**Tech Stack:** bash, ksql (KingbaseES CLI), sys_stat_statements, sys_stat_activity, SSH test framework

## Global Constraints

- DB 连接：`ksql -p 54321 -U system test -AXtc "..."` 封装在 `ksql_q()` 中，所有 SQL 通过它执行
- 视图前缀 `sys_`（非 pg_）：sys_stat_statements, sys_stat_activity, sys_stat_bgwriter
- 布尔值返回 `true`/`false`，比较前必须 `tr -d '[:space:]'`
- 测试文件放 `test/cases/test_stmt.sh`，通过 `bash test/run_tests.sh stmt` 运行
- 每次改完 lib/*.sh 后必须 `bash build.sh` 重新打包 dist/kbdiag 才能跑测试
- 输出字符串（汉字）直接写入 shell，无需 gettext
- `_finding()` 是 diagnose 的 finding 收集器：`_finding LEVEL category "body"`
- `OUTPUT_FMT` 全局变量控制输出格式（text/json）
- `TOP_N` 全局变量（默认 10）控制结果行数
- `KB_SLOW_THRESHOLD` 控制慢查询阈值（默认 5 秒）

---

### Task 1: `lib/cmd_stmt.sh` — 无参数多维 Top N 路径

**Files:**
- Create: `lib/cmd_stmt.sh`
- Modify: `build.sh`（lib 列表 + dispatch + USAGE）
- Rebuild: `dist/kbdiag`
- Create: `test/cases/test_stmt.sh`（本 Task 写前 3 个测试）

**Interfaces:**
- Produces: `cmd_stmt()` 函数，接受可选位置参数 `$1`（queryid）；无参数时走 overview 路径
- Produces: `_stmt_check_ext()` 内部帮助函数，检查 sys_stat_statements 是否可用，返回 0=可用 1=不可用

- [ ] **Step 1: 写 3 个失败测试**

创建 `test/cases/test_stmt.sh`：

```bash
test_stmt_exit_zero() {
  local code; code=$(ssh_node1_exit "$KBDIAG_REMOTE stmt")
  assert_exit_code 0 "$code"
}

test_stmt_has_four_sections() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stmt")
  assert_contains "$out" "平均耗时"
  assert_contains "$out" "总耗时"
  assert_contains "$out" "IO"
  assert_contains "$out" "调用频率"
}

test_stmt_has_stats_reset() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stmt")
  assert_contains "$out" "数据覆盖"
}
```

- [ ] **Step 2: 验证测试失败**

```bash
bash build.sh
bash test/run_tests.sh stmt
```
预期：`[FAIL] test_stmt_exit_zero: ...Unknown command: stmt` 或类似错误

- [ ] **Step 3: 创建 `lib/cmd_stmt.sh`**

```bash
# cmd_stmt.sh — SQL 历史统计（sys_stat_statements AWR 风格）

_stmt_check_ext() {
  local cnt
  cnt=$(ksql_q "SELECT count(*) FROM sys_catalog.sys_class WHERE relname='sys_stat_statements';" 2>/dev/null | tr -d '[:space:]')
  [[ "${cnt:-0}" -gt 0 ]]
}

_stmt_overview() {
  local stats_reset
  stats_reset=$(ksql_q "SELECT coalesce(stats_reset::text, 'unknown') FROM sys_stat_bgwriter;" 2>/dev/null | tr -d '[:space:]')

  printf "\n${BOLD}==> SQL 历史统计  [数据覆盖: %s → 现在]${RESET}\n\n" "$stats_reset"

  printf "${BOLD}── Top %d by 平均耗时 ──────────────────────────────────────────────${RESET}\n" "$TOP_N"
  printf " # | queryid           | calls    | mean_ms   | max_ms    | %%total | SQL 摘要\n"
  ksql_q "
    SELECT row_number() OVER (ORDER BY mean_exec_time DESC)::text,
           queryid::text,
           calls::text,
           round(mean_exec_time::numeric, 1)::text,
           round(max_exec_time::numeric, 1)::text,
           round(100.0 * total_exec_time / NULLIF(sum(total_exec_time) OVER (), 0), 1)::text || '%',
           left(replace(query, E'\n', ' '), 60)
    FROM sys_stat_statements
    WHERE calls > 0
    ORDER BY mean_exec_time DESC
    LIMIT ${TOP_N};" 2>/dev/null | column -t -s '|' || true

  echo ""
  printf "${BOLD}── Top %d by 总耗时 (calls × mean) ──────────────────────────────────${RESET}\n" "$TOP_N"
  printf " # | queryid           | calls     | total_s   | mean_ms | %%total | SQL 摘要\n"
  ksql_q "
    SELECT row_number() OVER (ORDER BY total_exec_time DESC)::text,
           queryid::text,
           calls::text,
           round((total_exec_time/1000.0)::numeric, 1)::text,
           round(mean_exec_time::numeric, 1)::text,
           round(100.0 * total_exec_time / NULLIF(sum(total_exec_time) OVER (), 0), 1)::text || '%',
           left(replace(query, E'\n', ' '), 60)
    FROM sys_stat_statements
    WHERE calls > 0
    ORDER BY total_exec_time DESC
    LIMIT ${TOP_N};" 2>/dev/null | column -t -s '|' || true

  echo ""
  printf "${BOLD}── Top %d by IO（cache miss，shared_blks_read）────────────────────${RESET}\n" "$TOP_N"
  printf " # | queryid           | calls  | blks_read  | hit_rate | SQL 摘要\n"
  ksql_q "
    SELECT row_number() OVER (ORDER BY shared_blks_read DESC)::text,
           queryid::text,
           calls::text,
           shared_blks_read::text,
           round(100.0 * shared_blks_hit / NULLIF(shared_blks_hit + shared_blks_read, 0), 1)::text || '%',
           left(replace(query, E'\n', ' '), 60)
    FROM sys_stat_statements
    WHERE calls > 0
    ORDER BY shared_blks_read DESC
    LIMIT ${TOP_N};" 2>/dev/null | column -t -s '|' || true

  echo ""
  printf "${BOLD}── Top %d by 调用频率 ──────────────────────────────────────────────${RESET}\n" "$TOP_N"
  printf " # | queryid           | calls     | mean_ms | SQL 摘要\n"
  ksql_q "
    SELECT row_number() OVER (ORDER BY calls DESC)::text,
           queryid::text,
           calls::text,
           round(mean_exec_time::numeric, 1)::text,
           left(replace(query, E'\n', ' '), 60)
    FROM sys_stat_statements
    WHERE calls > 0
    ORDER BY calls DESC
    LIMIT ${TOP_N};" 2>/dev/null | column -t -s '|' || true

  echo ""
}

_stmt_detail() {
  local queryid="$1"
  local row
  row=$(ksql_q "
    SELECT calls, mean_exec_time, min_exec_time, max_exec_time, stddev_exec_time,
           mean_plan_time,
           shared_blks_hit, shared_blks_read,
           round(100.0 * shared_blks_hit / NULLIF(shared_blks_hit + shared_blks_read, 0), 1) AS hit_ratio,
           temp_blks_read, temp_blks_written,
           query
    FROM sys_stat_statements
    WHERE queryid::text = '${queryid}';" 2>/dev/null || true)

  if [[ -z "$row" ]]; then
    warn "未找到 queryid=${queryid}"
    return 0
  fi

  local calls mean_ms min_ms max_ms stddev_ms plan_ms blks_hit blks_read hit_ratio temp_r temp_w sql_text
  IFS='|' read -r calls mean_ms min_ms max_ms stddev_ms plan_ms blks_hit blks_read hit_ratio temp_r temp_w sql_text <<< "$row"

  printf "\n${BOLD}==> SQL 详情  queryid: %s${RESET}\n\n" "$queryid"

  printf "${BOLD}── 执行统计 ──────────────────────────────────────────────────────────${RESET}\n"
  printf "调用次数    : %s\n" "${calls// /}"
  printf "平均耗时    : %s ms\n" "${mean_ms// /}"
  printf "最小 / 最大 : %s ms / %s ms\n" "${min_ms// /}" "${max_ms// /}"

  local stddev_note=""
  if awk "BEGIN{exit !(${stddev_ms// /} + 0 > ${mean_ms// /} + 0 * 0.5 + 0)}"; then
    stddev_note="  ← 高标准差，执行时间不稳定"
  fi
  printf "标准差      : %s ms%s\n" "${stddev_ms// /}" "$stddev_note"
  printf "规划时间    : %s ms (avg)\n" "${plan_ms// /}"
  echo ""

  printf "${BOLD}── IO 分解 ───────────────────────────────────────────────────────────${RESET}\n"
  printf "shared_blks_hit   : %s\n" "${blks_hit// /}"
  printf "shared_blks_read  : %s\n" "${blks_read// /}"

  local hit_note=""
  if awk "BEGIN{exit !(${hit_ratio// /} + 0 < ${KB_WARN_HIT})}"; then
    hit_note="  ← 低于正常水平"
  fi
  printf "Buffer 命中率     : %s%%%s\n" "${hit_ratio// /}" "$hit_note"
  printf "temp_blks_read    : %s\n" "${temp_r// /}"
  printf "temp_blks_written : %s\n" "${temp_w// /}"
  echo ""

  printf "${BOLD}── SQL 全文 ──────────────────────────────────────────────────────────${RESET}\n"
  echo "$sql_text"
  echo ""

  local explain_sql
  explain_sql=$(printf '%s' "$sql_text" | sed "s/\\\$\([0-9][0-9]*\)/'param_\1'  -- 替换为实际值/g")
  printf "${BOLD}── 验证路径 ──────────────────────────────────────────────────────────${RESET}\n"
  printf "执行计划（填入实际参数后运行）:\n"
  printf "  EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)\n"
  echo "$explain_sql" | sed 's/^/  /'
  echo ""
  printf "注意: SQL 中 \$N 为占位符，已替换为 'param_N'，运行前请换成真实值\n"
}

cmd_stmt() {
  local queryid="${1:-}"

  if ! _stmt_check_ext; then
    if [[ "$OUTPUT_FMT" == "json" ]]; then
      echo '{"command":"stmt","error":"sys_stat_statements not available","hint":"shared_preload_libraries = '"'"'sys_stat_statements'"'"' (需重启)"}'
    else
      warn "sys_stat_statements 扩展未启用"
      echo "  启用方式: shared_preload_libraries = 'sys_stat_statements'  (需重启)"
    fi
    return 0
  fi

  if [[ -n "$queryid" ]]; then
    _stmt_detail "$queryid"
  else
    _stmt_overview
  fi
}
```

- [ ] **Step 4: 修改 `build.sh` — 加入 lib 列表 + dispatch + USAGE**

在 `lib/cmd_diagnose.sh` 前插入 `lib/cmd_stmt.sh`（第 23 行前）：

```bash
# 原来的:
            lib/cmd_advisor.sh \
            lib/cmd_diagnose.sh \
# 改为:
            lib/cmd_advisor.sh \
            lib/cmd_stmt.sh \
            lib/cmd_diagnose.sh \
```

在 dispatch 的 `diagnose)` 行后加 `stmt)` 分支：

```bash
# 原来:
  diagnose)   cmd_diagnose ${SUBCMD:+"$SUBCMD"} "${CMD_ARGS[@]+"${CMD_ARGS[@]}"}" ;;
# 改为:
  stmt)       cmd_stmt "$SUBCMD" ;;
  diagnose)   cmd_diagnose ${SUBCMD:+"$SUBCMD"} "${CMD_ARGS[@]+"${CMD_ARGS[@]}"}" ;;
```

在 USAGE 的 `[DBA]` 区块加入 stmt 行（在 `sql` 行附近）：

```
  stmt [queryid]      SQL 历史统计 AWR 报告（Top N by mean/total/IO/calls）
```

- [ ] **Step 5: 重新打包**

```bash
bash build.sh
```
预期：`Built: dist/kbdiag`

- [ ] **Step 6: 运行测试，确认 3 个通过**

```bash
bash test/run_tests.sh stmt
```
预期：
```
[PASS] test_stmt_exit_zero
[PASS] test_stmt_has_four_sections
[PASS] test_stmt_has_stats_reset
Results: 3 passed, 0 failed (total: 3)
```

若 sys_stat_statements 未启用，test_stmt_has_four_sections 和 test_stmt_has_stats_reset 会 FAIL（扩展未启用时输出 WARN）。此时先跳过这两个测试，继续后续 Task。

- [ ] **Step 7: Commit**

```bash
git add lib/cmd_stmt.sh build.sh dist/kbdiag test/cases/test_stmt.sh
git commit -m "feat: add cmd_stmt overview path with four Top N sections"
```

---

### Task 2: stmt queryid 下钻 + JSON 输出 + 扩展不可用降级

**Files:**
- Modify: `test/cases/test_stmt.sh`（追加 4 个测试）
- No new files（_stmt_detail 和 JSON 逻辑已在 Task 1 的 cmd_stmt.sh 中实现，本 Task 补 JSON 路径）
- Modify: `lib/cmd_stmt.sh`（补 JSON 输出路径）
- Rebuild: `dist/kbdiag`

**Interfaces:**
- Consumes: `_stmt_check_ext()`, `_stmt_detail()`, `_stmt_overview()` from Task 1
- Produces: `--format json` 时 stmt 命令输出合法 JSON

- [ ] **Step 1: 追加 4 个测试到 `test/cases/test_stmt.sh`**

```bash
test_stmt_queryid_exit_zero() {
  # 用一个不存在的 queryid，应该 exit 0 并输出 warn
  local code; code=$(ssh_node1_exit "$KBDIAG_REMOTE stmt 0000000000000000")
  assert_exit_code 0 "$code"
}

test_stmt_queryid_has_explain_template() {
  # 先拿一个真实 queryid
  local qid
  qid=$(ssh_node1 "/home/kingbase/cluster/install/kingbase/bin/ksql -p 54321 -U system test -AXtc \"SELECT queryid::text FROM sys_stat_statements LIMIT 1;\"" 2>/dev/null | tr -d '[:space:]')
  [[ -z "$qid" ]] && { echo "  [SKIP] sys_stat_statements empty"; _pass; return; }
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE stmt $qid")
  assert_contains "$out" "验证路径"
  assert_contains "$out" "EXPLAIN"
}

test_stmt_json_valid() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE --format json stmt")
  assert_json_valid "$out"
  assert_contains "$out" '"command":"stmt"'
}

test_stmt_no_extension_graceful() {
  # 模拟扩展不可用：用 --format json 时应输出含 error 字段的 JSON，不 crash
  # 实际上如果扩展存在这个测试会通过 json_valid；如果不存在会验证 error 字段
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE --format json stmt" || true)
  # 必须是 valid JSON 或者不含 bash error
  assert_not_contains "$out" ": line "
  assert_not_contains "$out" "command not found"
}
```

- [ ] **Step 2: 运行测试确认初始状态（可能 FAIL）**

```bash
bash test/run_tests.sh stmt
```
预期：test_stmt_json_valid FAIL（JSON 路径未实现）

- [ ] **Step 3: 修改 `lib/cmd_stmt.sh`，补全 JSON 路径**

在 `cmd_stmt()` 函数的 `_stmt_overview` 和 `_stmt_detail` 调用前，增加 JSON 分支：

```bash
cmd_stmt() {
  local queryid="${1:-}"

  if ! _stmt_check_ext; then
    if [[ "$OUTPUT_FMT" == "json" ]]; then
      echo '{"command":"stmt","error":"sys_stat_statements not available","hint":"shared_preload_libraries = '"'"'sys_stat_statements'"'"' (需重启)"}'
    else
      warn "sys_stat_statements 扩展未启用"
      echo "  启用方式: shared_preload_libraries = 'sys_stat_statements'  (需重启)"
    fi
    return 0
  fi

  if [[ "$OUTPUT_FMT" == "json" ]]; then
    _stmt_json "$queryid"
    return 0
  fi

  if [[ -n "$queryid" ]]; then
    _stmt_detail "$queryid"
  else
    _stmt_overview
  fi
}
```

在 `cmd_stmt` 函数前，新增 `_stmt_json()` 函数：

```bash
_stmt_json() {
  local queryid="$1"
  local stats_reset
  stats_reset=$(ksql_q "SELECT coalesce(stats_reset::text,'') FROM sys_stat_bgwriter;" 2>/dev/null | tr -d '[:space:]')
  local ts; ts=$(date -u +%Y-%m-%dT%H:%M:%SZ)

  if [[ -n "$queryid" ]]; then
    # single queryid detail
    local row
    row=$(ksql_q "
      SELECT calls, mean_exec_time, min_exec_time, max_exec_time, stddev_exec_time,
             mean_plan_time, shared_blks_hit, shared_blks_read,
             round(100.0 * shared_blks_hit / NULLIF(shared_blks_hit + shared_blks_read, 0), 1),
             temp_blks_read, temp_blks_written,
             replace(replace(query, '\\', '\\\\'), '\"', '\\\"')
      FROM sys_stat_statements WHERE queryid::text = '${queryid}';" 2>/dev/null || true)
    if [[ -z "$row" ]]; then
      echo "{\"command\":\"stmt\",\"queryid\":\"${queryid}\",\"error\":\"not found\"}"
      return
    fi
    local calls mean_ms min_ms max_ms stddev_ms plan_ms blks_hit blks_read hit_ratio temp_r temp_w sql_text
    IFS='|' read -r calls mean_ms min_ms max_ms stddev_ms plan_ms blks_hit blks_read hit_ratio temp_r temp_w sql_text <<< "$row"
    printf '{"command":"stmt","queryid":"%s","stats_reset":"%s","timestamp":"%s","calls":%s,"mean_exec_time":%s,"min_exec_time":%s,"max_exec_time":%s,"stddev_exec_time":%s,"mean_plan_time":%s,"shared_blks_hit":%s,"shared_blks_read":%s,"hit_ratio":%s,"temp_blks_read":%s,"temp_blks_written":%s,"query":"%s"}\n' \
      "${queryid}" "${stats_reset}" "${ts}" \
      "${calls// /}" "${mean_ms// /}" "${min_ms// /}" "${max_ms// /}" "${stddev_ms// /}" "${plan_ms// /}" \
      "${blks_hit// /}" "${blks_read// /}" "${hit_ratio// /}" "${temp_r// /}" "${temp_w// /}" \
      "${sql_text}"
  else
    # overview: top_by_mean only in JSON (concise)
    local top_mean=""
    while IFS='|' read -r qid calls mean_ms max_ms pct summary; do
      [[ -z "${qid// /}" ]] && continue
      local entry
      entry=$(printf '{"queryid":"%s","calls":%s,"mean_exec_time":%s,"max_exec_time":%s,"pct_total":"%s","query_summary":"%s"}' \
        "${qid// /}" "${calls// /}" "${mean_ms// /}" "${max_ms// /}" "${pct// /}" \
        "$(echo "${summary// /}" | sed 's/"/\\"/g')")
      top_mean="${top_mean:+${top_mean},}${entry}"
    done < <(ksql_q "
      SELECT queryid::text, calls,
             round(mean_exec_time::numeric,1), round(max_exec_time::numeric,1),
             round(100.0*total_exec_time/NULLIF(sum(total_exec_time) OVER(),0),1),
             left(replace(query, E'\n', ' '), 60)
      FROM sys_stat_statements WHERE calls > 0
      ORDER BY mean_exec_time DESC LIMIT ${TOP_N};" 2>/dev/null || true)
    printf '{"command":"stmt","stats_reset":"%s","timestamp":"%s","top_by_mean":[%s]}\n' \
      "${stats_reset}" "${ts}" "${top_mean}"
  fi
}
```

- [ ] **Step 4: 重新打包**

```bash
bash build.sh
```

- [ ] **Step 5: 运行所有 stmt 测试，7 个全过**

```bash
bash test/run_tests.sh stmt
```
预期：
```
[PASS] test_stmt_exit_zero
[PASS] test_stmt_has_four_sections
[PASS] test_stmt_has_stats_reset
[PASS] test_stmt_queryid_exit_zero
[PASS] test_stmt_queryid_has_explain_template
[PASS] test_stmt_json_valid
[PASS] test_stmt_no_extension_graceful
Results: 7 passed, 0 failed (total: 7)
```

- [ ] **Step 6: Commit**

```bash
git add lib/cmd_stmt.sh build.sh dist/kbdiag test/cases/test_stmt.sh
git commit -m "feat: complete cmd_stmt with queryid drill-down, JSON output, extension guard"
```

---

### Task 3: diagnose `_diag_slow_queries` 历史均值关联

**Files:**
- Modify: `lib/cmd_diagnose.sh`（_diag_slow_queries 函数）
- Rebuild: `dist/kbdiag`
- Modify: `test/cases/test_diagnose.sh`（追加 1 个测试）

**Interfaces:**
- Consumes: `ksql_q()`, `_finding()`, `KB_SLOW_THRESHOLD`, `TOP_N`
- Produces: 当发现慢 SQL 且 sys_stat_statements 可用时，finding body 包含 "历史均值" 和"偶发/一贯"判断

- [ ] **Step 1: 追加 1 个测试到 `test/cases/test_diagnose.sh`**

```bash
test_diagnose_slow_hist_no_crash() {
  # diagnose 运行后不应含 bash 错误，不管是否有慢查询
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE diagnose" || true)
  assert_not_contains "$out" ": line "
  assert_not_contains "$out" "unbound variable"
  assert_not_contains "$out" "command not found"
}
```

- [ ] **Step 2: 运行确认当前通过（此测试验证不崩溃，理应已通过）**

```bash
bash test/run_tests.sh diagnose
```
记录当前通过数量，确认 test_diagnose_slow_hist_no_crash 通过。

- [ ] **Step 3: 修改 `lib/cmd_diagnose.sh` — 替换 `_diag_slow_queries`**

用以下内容完整替换 `_diag_slow_queries` 函数（lib/cmd_diagnose.sh 第 174-201 行）：

```bash
_diag_slow_queries() {
  local rows
  rows=$(ksql_q "
    SELECT pid, usename,
           date_trunc('second', now()-query_start)::text AS duration,
           EXTRACT(EPOCH FROM (now()-query_start))::int AS dur_sec,
           coalesce(wait_event_type,'') AS wait_event_type,
           coalesce(wait_event,'') AS wait_event,
           left(query, 80) AS query
    FROM sys_stat_activity
    WHERE state='active'
      AND now()-query_start > interval '${KB_SLOW_THRESHOLD} seconds'
      AND pid <> sys_backend_pid()
    ORDER BY query_start
    LIMIT ${TOP_N};" 2>/dev/null || true)

  [[ -z "$rows" ]] && return

  # Collect pids for stmt correlation
  local pid_list=""
  local cnt=0 evidence=""
  while IFS='|' read -r pid user duration dur_sec wtype wevent query; do
    [[ -z "${pid// /}" ]] && continue
    cnt=$(( cnt + 1 ))
    evidence="${evidence}    · pid ${pid// /} (${user// /}) | 时长: ${duration// /} | wait: ${wtype// /}/${wevent// /}\n"
    evidence="${evidence}      SQL: $(echo "$query" | xargs 2>/dev/null || echo "$query")\n"
    pid_list="${pid_list:+${pid_list},}${pid// /}"
  done <<< "$rows"

  # Try sys_stat_statements correlation (best-effort, skip if unavailable)
  local stmt_avail
  stmt_avail=$(ksql_q "SELECT count(*) FROM sys_catalog.sys_class WHERE relname='sys_stat_statements';" 2>/dev/null | tr -d '[:space:]')

  local hist_note=""
  if [[ "${stmt_avail:-0}" -gt 0 && -n "$pid_list" ]]; then
    # Get query_id for each slow pid (query_id column available in KingbaseES v9+)
    local qid_rows
    qid_rows=$(ksql_q "
      SELECT pid::text, coalesce(query_id::text,'')
      FROM sys_stat_activity
      WHERE pid = ANY(ARRAY[${pid_list}]);" 2>/dev/null || true)

    local qid_csv=""
    if [[ -n "$qid_rows" ]]; then
      while IFS='|' read -r pid qid; do
        [[ -z "${qid// /}" || "${qid// /}" == "0" ]] && continue
        qid_csv="${qid_csv:+${qid_csv},'}'${qid// /}'"
      done <<< "$qid_rows"
      # rebuild properly quoted
      qid_csv=""
      while IFS='|' read -r pid qid; do
        [[ -z "${qid// /}" || "${qid// /}" == "0" ]] && continue
        qid_csv="${qid_csv:+${qid_csv},}'${qid// /}'"
      done <<< "$qid_rows"
    fi

    if [[ -n "$qid_csv" ]]; then
      local hist_rows
      hist_rows=$(ksql_q "
        SELECT queryid::text, calls, mean_exec_time, stddev_exec_time
        FROM sys_stat_statements
        WHERE queryid::text IN (${qid_csv});" 2>/dev/null || true)

      if [[ -n "$hist_rows" ]]; then
        hist_note="  历史均值关联 (sys_stat_statements):\n"
        while IFS='|' read -r qid calls hist_mean hist_stddev; do
          [[ -z "${qid// /}" ]] && continue
          local qid_s="${qid// /}" hist_mean_s="${hist_mean// /}" hist_stddev_s="${hist_stddev// /}" calls_s="${calls// /}"
          # Find matching slow query duration
          local cur_dur_sec
          cur_dur_sec=$(ksql_q "
            SELECT EXTRACT(EPOCH FROM (now()-query_start))::int
            FROM sys_stat_activity WHERE query_id::text='${qid_s}'
            LIMIT 1;" 2>/dev/null | tr -d '[:space:]')
          [[ -z "${cur_dur_sec:-}" ]] && continue
          local cur_ms=$(( cur_dur_sec * 1000 ))
          local verdict
          if awk "BEGIN{exit !(${cur_ms} > ${hist_mean_s:-0} * 3)}"; then
            verdict="性能突降（异常）"
          else
            verdict="一贯慢（设计问题）"
          fi
          hist_note="${hist_note}    · queryid=${qid_s}  当前: ${cur_dur_sec}s  历史均值: $(awk "BEGIN{printf \"%.1f\", ${hist_mean_s}/1000}")s  stddev: $(awk "BEGIN{printf \"%.1f\", ${hist_stddev_s:-0}/1000}")s  → ${verdict}\n"
          if [[ "$verdict" == "性能突降（异常）" ]]; then
            hist_note="${hist_note}    根因假设: 历史均值仅 $(awk "BEGIN{printf \"%.1f\", ${hist_mean_s}/1000}")s，突然变慢 → 统计信息过期、锁等待、数据量激增\n"
          fi
          hist_note="${hist_note}    验证: kbdiag stmt ${qid_s}\n"
        done <<< "$hist_rows"
      fi
    fi
  fi

  local body="慢查询\n  症状：当前 ${cnt} 条查询运行超过 ${KB_SLOW_THRESHOLD}s\n  证据:\n${evidence}${hist_note}  建议:\n    · kbdiag sql <pid> 查看执行计划\n    · kbdiag stmt <queryid> 查看历史统计"
  _finding "INFO" "slow_queries" "$body"
}
```

- [ ] **Step 4: 重新打包**

```bash
bash build.sh
```

- [ ] **Step 5: 运行 diagnose 测试集，确认全部通过**

```bash
bash test/run_tests.sh diagnose
```
预期：所有原有测试仍通过，test_diagnose_slow_hist_no_crash 通过。

- [ ] **Step 6: Commit**

```bash
git add lib/cmd_diagnose.sh dist/kbdiag test/cases/test_diagnose.sh
git commit -m "feat: correlate slow queries with sys_stat_statements history in diagnose"
```

---

### Task 4: diagnose `_diag_stmt_top`（--full 模式 Top 3 历史负担）

**Files:**
- Modify: `lib/cmd_diagnose.sh`（新增 `_diag_stmt_top`，追加到 full_mode 调用列表）
- Rebuild: `dist/kbdiag`
- Modify: `test/cases/test_diagnose.sh`（追加 1 个测试）

**Interfaces:**
- Consumes: `ksql_q()`, `_finding()`, `OUTPUT_FMT`
- Produces: `--full` 模式下，若 sys_stat_statements 可用且有数据，生成 INFO 级别 "stmt_top" finding

- [ ] **Step 1: 追加测试到 `test/cases/test_diagnose.sh`**

```bash
test_diagnose_full_stmt_top_no_crash() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE diagnose --full" || true)
  assert_not_contains "$out" ": line "
  assert_not_contains "$out" "unbound variable"
  # If stmt top finding appeared, it should contain expected text
  if echo "$out" | grep -q "历史 SQL 负担"; then
    assert_contains "$out" "kbdiag stmt"
  fi
  _pass 2>/dev/null || true  # already handled above
}
```

注意：由于 _pass 可能已被上面的 assert 调用，需要确保测试计数正确。改用更简单的写法：

```bash
test_diagnose_full_stmt_top_no_crash() {
  local out; out=$(ssh_node1 "$KBDIAG_REMOTE diagnose --full" || true)
  assert_not_contains "$out" ": line "
  assert_not_contains "$out" "unbound variable"
}
```

- [ ] **Step 2: 运行确认当前状态**

```bash
bash test/run_tests.sh diagnose
```

- [ ] **Step 3: 新增 `_diag_stmt_top` 函数到 `lib/cmd_diagnose.sh`**

在 `_diag_render` 函数前（第 416 行 `# ── 渲染` 注释前）插入：

```bash
_diag_stmt_top() {
  local stmt_avail
  stmt_avail=$(ksql_q "SELECT count(*) FROM sys_catalog.sys_class WHERE relname='sys_stat_statements';" 2>/dev/null | tr -d '[:space:]')
  [[ "${stmt_avail:-0}" -eq 0 ]] && return

  local stats_reset
  stats_reset=$(ksql_q "SELECT coalesce(stats_reset::date::text,'unknown') FROM sys_stat_bgwriter;" 2>/dev/null | tr -d '[:space:]')

  local rows
  rows=$(ksql_q "
    SELECT queryid::text,
           round((total_exec_time/1000.0)::numeric,1)::text AS total_s,
           round(100.0*total_exec_time/NULLIF(sum(total_exec_time) OVER(),0),1)::text AS pct,
           calls::text,
           round(mean_exec_time::numeric,1)::text AS mean_ms,
           left(replace(query, E'\n', ' '), 70) AS summary
    FROM sys_stat_statements
    WHERE calls > 0
    ORDER BY total_exec_time DESC
    LIMIT 3;" 2>/dev/null || true)

  [[ -z "$rows" ]] && return

  local evidence=""
  while IFS='|' read -r qid total_s pct calls mean_ms summary; do
    [[ -z "${qid// /}" ]] && continue
    evidence="${evidence}  · queryid=${qid// /}  总耗时: ${total_s// /}s (${pct// /}%)  calls: ${calls// /}  mean: ${mean_ms// /}ms\n"
    evidence="${evidence}    ${summary}\n"
  done <<< "$rows"

  local body="历史 SQL 负担 Top 3（累计总耗时，stats_reset: ${stats_reset}）\n${evidence}  验证: kbdiag stmt（查看完整 AWR 报告）"
  _finding "INFO" "stmt_top" "$body"
}
```

- [ ] **Step 4: 在 `cmd_diagnose()` 的 full_mode 块中追加 `_diag_stmt_top` 调用**

找到 lib/cmd_diagnose.sh 中的 `_diag_temp` 行（full_mode 块最后一行），在其后追加：

```bash
  if [[ $full_mode -eq 1 ]]; then
    _diag_vacuum_debt
    _diag_indexes
    _diag_params
    _diag_bloat
    _diag_buffer_hit
    _diag_checkpoint
    _diag_temp
    _diag_stmt_top      # ← 追加这行
  fi
```

- [ ] **Step 5: 重新打包**

```bash
bash build.sh
```

- [ ] **Step 6: 运行完整测试集**

```bash
bash test/run_tests.sh diagnose
bash test/run_tests.sh stmt
```
预期：所有测试通过。

- [ ] **Step 7: Commit**

```bash
git add lib/cmd_diagnose.sh dist/kbdiag test/cases/test_diagnose.sh
git commit -m "feat: add _diag_stmt_top to diagnose --full, show Top 3 SQL by total exec time"
```

---

### Task 5: 完整测试 + git push

**Files:**
- No new files

- [ ] **Step 1: 运行所有测试套件**

```bash
bash test/run_tests.sh
```
预期：全部通过，包含：
- test_stmt.sh: 7 个测试
- test_diagnose.sh: 原有 6 个 + 新增 2 个 = 8 个

- [ ] **Step 2: 修复失败（如有）**

若 test_stmt_has_four_sections 失败（sys_stat_statements 在 VM 未启用）：
- SSH 到 node1 验证：`ssh -p 57103 kevin@127.0.0.1 "sudo -i -u kingbase /home/kingbase/cluster/install/kingbase/bin/ksql -p 54321 -U system test -AXtc 'SELECT count(*) FROM sys_stat_statements;'"`
- 若扩展确实未启用，测试预期修改为检查 WARN 输出

- [ ] **Step 3: Push**

```bash
git push
```

---

## 自检（spec 覆盖对照）

| Spec 要求 | 覆盖 Task |
|-----------|-----------|
| stmt 无参数 AWR 四区块（mean/total/IO/calls Top N） | Task 1 |
| stmt 展示 stats_reset 时间戳 | Task 1 |
| stmt queryid 下钻，显示执行统计 + IO 分解 | Task 1（_stmt_detail 已实现）|
| stmt queryid 下钻，输出 EXPLAIN 模板（$N → param_N）| Task 1（_stmt_detail 已实现）|
| stmt --format json 无参数输出含 command/stats_reset/top_by_mean | Task 2 |
| stmt --format json queryid 输出完整字段 | Task 2 |
| 扩展未启用时优雅降级（text: WARN，json: error 字段）| Task 1 + Task 2 |
| diagnose slow_queries 增加历史均值关联，输出"偶发/一贯"判断 | Task 3 |
| diagnose 无慢查询时跳过 stmt 查询 | Task 3（早返回逻辑）|
| diagnose --full 新增 _diag_stmt_top Top 3 负担 | Task 4 |
| _diag_stmt_top 找不到视图时跳过不报错 | Task 4（_stmt_check_ext 逻辑）|
| build.sh 接入 cmd_stmt.sh + dispatch + USAGE | Task 1 |
| test/cases/test_stmt.sh 7 个测试 | Task 1 + Task 2 |
