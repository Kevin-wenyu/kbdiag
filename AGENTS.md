# AGENTS.md

本项目由 Claude Code 和 Codex 共同开发，两边遵守同一套规则，规则的唯一来源是 `CLAUDE.md`，开工前先读它。

- 唯一的计划入口是 `.omc/plans/`（最多 1 个 `.md`）；不要在 `.omx/plans/` 等工具私有目录另建计划。
- 过程记录写进 `chronicle/YYYY-MM-DD.md`（追加，不新建同日文件）。
- Codex 侧的技能工作流（OMX）对应 `CLAUDE.md` 里的 OMC 主线：plan → execute → review → verify。
